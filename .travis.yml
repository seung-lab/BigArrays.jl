language: julia
os:
    - linux
julia: 
    - 0.7
    - nightly

sudo: required 

notifications:
    email: false

script:
    - sudo apt-get install -y hdf5-tools 
    - julia -e 'import Pkg; Pkg.add(pwd()); Pkg.test("BigArrays")'

matrix:
    allow_failures:
        - julia: nightly
